[project]
name = "dubbing-studio"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
dubbing-cli = "dubbing.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100
